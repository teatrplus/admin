# PocketBase API origin used by the Vite app.
# Must match `pocketbase serve --http=...` — the code default is 8090,
# which is wrong if another process already owns that port.
VITE_POCKETBASE_URL=http://127.0.0.1:8090

